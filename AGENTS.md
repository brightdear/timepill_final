# Workspace Instructions

## ML Tasks

When the user asks about machine learning, training, datasets, model design, evaluation, inference, augmentation, or deployment:

1. Read `ml/ml_prompt.md` first.
2. Read `ml/ml_context.md` next.
3. Use those files as the default local guidance before answering or editing ML-related code.

If there is any conflict, prioritize direct user instructions over these local notes.
